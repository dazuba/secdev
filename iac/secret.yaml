apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  database-url: "${{ secrets.STAGING_DATABASE_URL || secrets.PRODUCTION_DATABASE_URL }}"
  jwt-secret: "${{ secrets.STAGING_JWT_SECRET || secrets.PRODUCTION_JWT_SECRET }}"

